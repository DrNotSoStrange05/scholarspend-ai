[project]
name = "scholarspend-ai"
version = "1.0.0"
description = "Financial stability tool for students"
requires-python = ">=3.11"

# Note: The actual backend configuration is in backend/pyproject.toml
# This file is kept for reference only.
# Render will deploy from the backend/ directory.

